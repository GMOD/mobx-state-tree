{
  "name": "@jbrowse/mobx-state-tree",
  "version": "6.5.0",
  "description": "Fork of mobx-state-tree starting from v5.4.2 for use in jbrowse 2",
  "types": "./dist/index.d.ts",
  "main": "./dist/mobx-state-tree.cjs",
  "module": "./dist/mobx-state-tree.mjs",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/mobx-state-tree.mjs",
      "require": "./dist/mobx-state-tree.cjs"
    },
    "./package.json": "./package.json"
  },
  "sideEffects": false,
  "scripts": {
    "lint": "eslint --report-unused-disable-directives --max-warnings 0",
    "clean": "rimraf dist lib",
    "prebuild": "pnpm clean",
    "format": "pnpm prettier --write .",
    "format:check": "pnpm prettier --check .",
    "build": "tsc && rollup -c",
    "test": "vitest",
    "test:types": "tsc -p __tests__/types/tsconfig.json",
    "test-ci": "pnpm test:types && pnpm test:dev --run && pnpm test:prod --run",
    "test:dev": "cross-env NODE_ENV=development vitest",
    "test:prod": "cross-env NODE_ENV=production vitest",
    "prebench": "./scripts/build-both-branches.sh",
    "bench": "cross-env NODE_ENV=production vitest bench --run",
    "preversion": "pnpm lint && pnpm format:check && pnpm build && pnpm test-ci",
    "version": "git-cliff --tag v$npm_package_version --unreleased --prepend CHANGELOG.md && git add CHANGELOG.md",
    "postversion": "git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GMOD/mobx-state-tree.git"
  },
  "author": "Michel Weststrate",
  "license": "MIT",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.0.1",
    "cross-env": "^10.1.0",
    "eslint": "^10.4.0",
    "eslint-plugin-import": "^2.32.0",
    "git-cliff": "^2.13.1",
    "mobx": "^7.0.0",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "rollup": "^4.60.4",
    "rollup-plugin-dts": "^6.4.1",
    "rollup-plugin-sourcemaps2": "^0.5.7",
    "spec.ts": "^1.1.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.4",
    "vitest": "^4.1.7"
  },
  "peerDependencies": {
    "mobx": "^7.0.0"
  },
  "keywords": [
    "mobx",
    "mobx-state-tree",
    "promise",
    "reactive",
    "frp",
    "functional-reactive-programming",
    "state management"
  ],
  "publishConfig": {
    "access": "public"
  }
}
